services:
  bot:
    build: .
    container_name: cv-screener-bot
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: Asia/Jakarta
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
